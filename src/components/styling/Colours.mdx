import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';
import { colours } from "../../styles/colours";

<Meta title="Colours" />

<ColorPalette>
  {Object.entries(colours.colours).map(([name, colour]) => (
    <ColorItem
      key={name}
      title={name}
      subtitle={colour}
      colors={[colour]}
    />
  ))}
</ColorPalette>