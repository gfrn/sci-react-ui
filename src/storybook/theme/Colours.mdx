import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';

import {BaseTheme} from "../../themes/BaseTheme";
import {DiamondTheme} from "../../themes/DiamondTheme";

export function ThemeColorItem({title, theme, colourSet, mode}) {
    return <ColorItem
        title={title}
        subtitle={""}
        colors={{
            "main": theme.colorSchemes[mode].palette[colourSet].main,
            "light": theme.colorSchemes[mode].palette[colourSet].light,
            "dark": theme.colorSchemes[mode].palette[colourSet].dark,
            "contrast text": theme.colorSchemes[mode].palette[colourSet].contrastText
        }}
    />
}

<Meta title="Theme/Colours" />

<div className="sb-container">
    <div className='sb-section-title'>
        # Theme Colours

        Theme colours available:

        * [Base Theme](#base)
        * [Diamond Theme](#diamond)
    </div>
</div>

<div className="sb-container">
    <div id="base" className='sb-section'>
        ## Base Theme

        <div className='sb-section-item'>
            ### Light Mode
            <ColorPalette>
                <ThemeColorItem title="Primary" theme={BaseTheme} colourSet="primary" mode="light" />
                <ThemeColorItem title="Secondary" theme={BaseTheme} colourSet="secondary" mode="light" />
            </ColorPalette>

            ### Dark Mode
            <ColorPalette>
                <ThemeColorItem title="Primary" theme={BaseTheme} colourSet="primary" mode={"dark"} />
                <ThemeColorItem title="Secondary" theme={BaseTheme} colourSet="secondary" mode={"dark"} />
            </ColorPalette>
        </div>
    </div>
</div>


<div className="sb-container">
    <div id="diamond" className='sb-section'>
        ## Diamond Theme

        <div className='sb-section-item'>
            ### Light Mode
            <ColorPalette>
                <ThemeColorItem title="Primary" theme={DiamondTheme} colourSet="primary" mode="light" />
                <ThemeColorItem title="Secodary" theme={DiamondTheme} colourSet="secondary" mode="light" />
            </ColorPalette>

            ### Dark Mode
            <ColorPalette>
                <ThemeColorItem title="Primary" theme={DiamondTheme} colourSet="primary" mode={"dark"} />
                <ThemeColorItem title="Secodary" theme={DiamondTheme} colourSet="secondary" mode={"dark"} />
            </ColorPalette>
        </div>
    </div>
</div>
